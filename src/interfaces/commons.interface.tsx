interface TableOptionsData {
    allowed: string[];
    prevUrl: string;
    id: number;
}

interface DefaultPageIdProps {
    params: {
        id: string;
    };
}
interface DefaultPageIdItemProps {
    params: {
        iditem: string;
        idcotizacion: string;
    };
}